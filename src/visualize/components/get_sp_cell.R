get_sp_cell <- function(raster ) {
  cat(blue("Acquiring number of species per cell.\n"))
  
  # Define the function to apply to each chunk

  
  
  cat(cc$lightGreen("Successfully acquired proportion of species in each cell. \n"))
}